# RAG 前端功能演示指南

## 快速开始

### 1. 启动后端服务

```bash
cd /Users/wangyubj15/project/langchain4j-demo/langchain4j-demo-core
mvn spring-boot:run
```

等待后端启动完成（看到 "Started Langchain4jDemoApplication" 信息）。

### 2. 启动前端服务

```bash
cd /Users/wangyubj15/project/langchain4j-demo/langchain4j-demo-frontend
npm run dev
```

访问前端地址（通常是 http://localhost:5173）。

## 功能演示流程

### 步骤 1: 加载文档到向量数据库

1. **找到加载按钮**
   - 在左侧边栏的设置区域
   - 绿色的"📚 加载文档到向量库"按钮

2. **点击按钮打开弹窗**
   - 弹窗标题：加载文档到向量数据库
   - 显示功能说明和输入框

3. **输入文档路径**

   使用项目自带的测试文档：
   ```
   /Users/wangyubj15/project/langchain4j-demo/test-documents
   ```

   或者使用相对路径（相对于后端项目根目录）：
   ```
   ./test-documents
   ```

4. **点击"加载文档"按钮**
   - 显示加载动画
   - 加载成功后显示绿色成功消息
   - 2秒后自动关闭弹窗
   - 聊天区域显示系统提示消息

### 步骤 2: 切换到 RAG 问答模式

1. **选择 RAG 模式**
   - 在左侧边栏的"聊天模式"区域
   - 点击"RAG 问答"按钮
   - 按钮变为紫色激活状态

2. **顶部显示当前模式**
   - 聊天区域顶部显示"当前模式: RAG 问答"

### 步骤 3: 基于文档进行问答

现在可以基于已加载的文档进行提问：

**示例问题 1: 基础问题**
```
问: 什么是 LangChain4j？
```

预期回答：基于 test-documents/sample.txt 中的内容，AI 会回答 LangChain4j 的定义和特性。

**示例问题 2: 详细信息**
```
问: LangChain4j 有哪些主要特性？
```

预期回答：列出 5 个主要特性（支持多种 LLM、聊天记忆、工具调用等）。

**示例问题 3: 流程相关**
```
问: RAG 的工作流程是什么？
```

预期回答：描述 6 个步骤（文档加载、分割、向量化、存储、检索、生成）。

**示例问题 4: 应用场景**
```
问: LangChain4j 可以用在什么场景？
```

预期回答：列出使用场景（知识库问答、智能客服等）。

### 步骤 4: 更新文档（可选）

当文档内容变化时：

1. **修改或添加新文档**
   - 在 `test-documents` 目录下添加新文件
   - 或修改现有文件内容

2. **重新加载文档**
   - 点击"📚 加载文档到向量库"
   - 输入相同的路径
   - 重新加载

3. **验证更新**
   - 提问新文档中的内容
   - 确认 AI 能够回答

## 界面预览

### 侧边栏
```
┌─────────────────────────────┐
│ 聊天模式                    │
│ [简单聊天]                  │
│ [记忆聊天]                  │
│ [流式聊天]                  │
│ [AI 助手]                   │
│ [RAG 问答] ← 当前选中       │
│ [工具调用]                  │
│ [MCP 工具]                  │
│                             │
│ 设置                        │
│ 会话ID: user-123            │
│ Temperature: 0.7            │
│ Max Tokens: 2000            │
│                             │
│ [清空对话]                  │
│ [📚 加载文档到向量库] ← 新增│
│ [查看本地工具]              │
│ [查看MCP工具]               │
└─────────────────────────────┘
```

### 文档加载弹窗
```
┌───────────────────────────────────────┐
│ 加载文档到向量数据库               × │
├───────────────────────────────────────┤
│                                       │
│ ℹ️ 将文档加载到向量数据库后，       │
│   可以使用 RAG 问答功能基于这些     │
│   文档进行问答。                     │
│                                       │
│ 文档路径                             │
│ ┌─────────────────────────────────┐ │
│ │ /path/to/documents              │ │
│ └─────────────────────────────────┘ │
│ 可以是文件路径或目录路径           │
│                                       │
│              [取消] [加载文档]       │
└───────────────────────────────────────┘
```

### 成功消息
```
┌───────────────────────────────────────┐
│ ✅ 文档已成功加载到向量数据库，      │
│    现在可以使用 RAG 问答模式进行    │
│    提问了！                          │
└───────────────────────────────────────┘
```

## 测试用例

### 测试 1: 正常加载流程
1. 输入: `/Users/wangyubj15/project/langchain4j-demo/test-documents`
2. 期望: 成功加载，显示成功消息
3. 验证: 询问"什么是 LangChain4j"，能正确回答

### 测试 2: 错误路径处理
1. 输入: `/invalid/path/to/documents`
2. 期望: 显示错误消息（红色背景）
3. 验证: 错误消息清晰易懂

### 测试 3: 空路径处理
1. 输入: 空字符串或只有空格
2. 期望: "加载文档"按钮禁用
3. 验证: 无法点击加载按钮

### 测试 4: 相对路径
1. 输入: `./test-documents`
2. 期望: 成功加载（相对于后端工作目录）
3. 验证: 能够正常问答

### 测试 5: 重复加载
1. 操作: 连续两次加载同一路径
2. 期望: 两次都成功，文档被覆盖
3. 验证: 问答结果一致

## 性能说明

### 加载时间
- 小文件（< 1MB）: 1-3 秒
- 中等文件（1-10MB）: 3-10 秒
- 大文件（> 10MB）: 10+ 秒

加载时间取决于：
- 文件大小
- 文件数量
- Embedding 模型速度
- 网络延迟（如使用远程 API）

### 内存使用
使用 `InMemoryEmbeddingStore`，所有向量数据存储在内存中：
- 服务重启后数据丢失
- 内存占用随文档量增加
- 适合演示和开发环境

## 注意事项

### 1. 路径格式
- **绝对路径**（推荐）: `/Users/username/documents`
- **相对路径**: `./test-documents`（相对于后端工作目录）
- **Windows**: `C:/Users/username/documents` 或 `C:\\Users\\username\\documents`

### 2. 文档格式支持
当前配置支持的格式（基于 Apache Tika）：
- 纯文本: `.txt`, `.md`
- PDF: `.pdf`
- Word: `.doc`, `.docx`
- 其他: 取决于 Tika 配置

### 3. 中文支持
- 文档内容完全支持中文
- Embedding 模型支持中文向量化
- 问答支持中文查询

### 4. 数据持久化
⚠️ 当前使用内存存储，服务重启后需要重新加载文档。

生产环境建议：
- 使用持久化向量数据库（Pinecone、Weaviate、Milvus 等）
- 配置自动加载初始文档
- 实现增量更新机制

## 故障排查

### 问题 1: 加载失败
**症状**: 显示错误消息"加载文档时发生错误"

**可能原因**:
1. 路径不存在或无权限
2. 文件格式不支持
3. 文件编码问题
4. 后端服务未启动

**解决方案**:
1. 检查路径是否正确
2. 确认文件可读
3. 查看后端日志
4. 验证后端服务运行状态

### 问题 2: RAG 问答无结果
**症状**: AI 回复"上下文中没有相关信息"

**可能原因**:
1. 文档未成功加载
2. 查询与文档内容相关性低
3. 相似度阈值设置过高

**解决方案**:
1. 确认文档加载成功
2. 调整查询方式
3. 降低 minScore 阈值（在后端代码中）

### 问题 3: 前端无法访问
**症状**: 浏览器无法打开 http://localhost:5173

**解决方案**:
1. 确认前端服务已启动
2. 检查端口是否被占用
3. 尝试访问 http://127.0.0.1:5173

### 问题 4: 后端 API 404
**症状**: 控制台显示 POST /api/rag/load 404

**解决方案**:
1. 确认后端服务已启动
2. 验证 API 端点存在
3. 检查前端代理配置

## 扩展功能建议

基于当前实现，可以进一步扩展：

### 1. 文档管理界面
```javascript
// 新增功能
- 查看已加载文档列表
- 删除特定文档
- 清空向量数据库
- 文档统计信息
```

### 2. 批量操作
```javascript
// 支持多路径
paths: [
  "/path/to/docs1",
  "/path/to/docs2",
  "/path/to/docs3"
]
```

### 3. 高级配置
```javascript
// 加载选项
{
  path: "/path/to/docs",
  recursive: true,        // 递归加载子目录
  extensions: [".txt", ".pdf"],  // 文件类型过滤
  maxFileSize: 10485760,  // 最大文件大小 (10MB)
  encoding: "UTF-8"       // 文件编码
}
```

### 4. 实时反馈
```javascript
// WebSocket 进度更新
onProgress: (progress) => {
  // { current: 5, total: 10, fileName: "doc5.txt" }
}
```

## 总结

现在你的前端应用已经具备完整的文档加载功能：

✅ 用户友好的界面
✅ 实时加载反馈
✅ 错误处理
✅ 系统消息提示
✅ 响应式设计
✅ 与 RAG 问答模式无缝集成

用户可以随时更新文档库，无需编写代码或使用命令行工具！
